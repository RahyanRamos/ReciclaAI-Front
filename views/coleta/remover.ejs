<%- include('../layouts/partials/page-title', {
    title: titulo,
    subtitle: subtitulo,
    current: 'Excluir coleta'
}) %>

<div class="row">
    <div class="col-lg-8 offset-lg-2">
        <div class="card border-danger">
            <div class="card-header bg-danger">
                <h4 class="card-title text-white mb-0">Confirmar exclusao</h4>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">Esta acao excluira o registro de forma permanente.</div>
                <table class="table table-bordered">
                    <tbody>
                        <tr><th>Data</th><td><%= dateFormat(coleta.dataColeta) %></td></tr>
                        <tr><th>Peso</th><td><%= coleta.pesoTotalEstimado.toFixed(2) %></td></tr>
                        <tr><th>Status</th><td><%= coleta.status %></td></tr>
                    </tbody>
                </table>
                <form action="/coletas/<%= coleta.id %>/excluir" method="POST">
                    <button type="submit" class="btn btn-danger mr-2">Confirmar exclusao</button>
                    <a href="/coletas" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
